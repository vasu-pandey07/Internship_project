# Frontend runtime variables
# Use your backend public API URL and include /api/v1 suffix
VITE_API_URL=https://your-backend-domain.com/api/v1

# Optional: set explicitly if your socket server URL differs from API origin
# VITE_SOCKET_URL=https://your-backend-domain.com
